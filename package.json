{
  "name": "test-local-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace for TestLocal test suites and utilities",
  "scripts": {
    "install:all": "pnpm -C web-tenant-tests install && pnpm -C file-viewer install && (cd fla-ui-tests && dotnet restore)"
  }
}
