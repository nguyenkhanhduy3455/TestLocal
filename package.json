{
  "name": "test-local-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace for TestLocal test suites and utilities",
  "scripts": {
    "install:all": "pnpm -C web-tenant-tests install && pnpm -C file-viewer install && pnpm -C web-tenant-tests exec playwright install && (cd fla-ui-tests && dotnet restore)",
    "test:web-tenant": "pnpm -C web-tenant-tests test",
    "test:ui": "pnpm -C web-tenant-tests run test:ui"
  }
}
